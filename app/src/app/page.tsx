export default function RootPage() {
  return (
    <main className="mx-auto max-w-(--container-content) p-8">
      <h1 className="mb-4 text-4xl font-[var(--font-display)]">CommonGround</h1>
      <p className="max-w-prose">
        Structured deliberation for small-group governance. This is the application shell; the
        marketing site lives at <code>site/</code>.
      </p>
    </main>
  );
}
